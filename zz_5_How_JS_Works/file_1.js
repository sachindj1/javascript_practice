// How JS works

// Browser provides window Object

// 1--------->  code compilation has three phases
 
// (interpretor compile each line execute each line), instead compiler first compile entire code
// we compile code first to check if there are any errors in code before its execution
// and to determine appropreate scope for variable (D.A.S.V)

//// A>  Tokenization
//// B>  Parsing  (AST-> Abstrac Syntax Tree)
//// c>  Code Genaration


// 2----------> code execution



// A> Global Execution Context
           // a--> creation phase(Global Memory)
               



          // b--> code execution phase
             // JS is a synchronous programming language (EXECUTING CODE LINE BY LINE , DOESENT EXECUTE SECOND LINE WITHOUT EXECUTING FIRST LINE)
             // the asynchronous functionality is provided by browser to JS








             //------------------------------------------------------------------


             //Global Context
             // Local Context


             // inside local context(function context) the aargument is stored inside 
             // array like object
             // example name('sachin' , 'jadhav'){cnsole.log(argument[0])}

             // argument is a array like object

